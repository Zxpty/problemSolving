#include <bits/stdc++.h>
#define cpu() ios::sync_with_stdio(false);cin.tie(nullptr);
using namespace std;
/* It's based in Kosaraju's Algorithm
 * SCC
 * Type of question:
 * -> number of SCC
 * -> print the SCC
 *  Only for directed graph
 */
int main() {
	cpu();
	
	return 0;
}

