#include <bits/stdc++.h>
#define cpu() ios::sync_with_stdio(false);cin.tie(nullptr);
using namespace std;
/*
 * A tree in which we have N nodes and N - 1 edges
 * All nodes are reachable from each other
 * If a problem give us a graph we can construct a tree according the constrains above
*/
int main() {
	cpu();
	
	return 0;
}

