#include <bits/stdc++.h>
#include <climits>
#ifdef LOCAL
#include "../debug.cpp"
#else
#define dbg(...)
#endif
#define cpu() ios::sync_with_stdio(false);cin.tie(nullptr);
using namespace std;
typedef long long ll;
const ll mod = 1e9+7;
void solve()
{
  int n; cin>>n;
  vector<ll> r(n);
  for(ll& x : r) cin>>x;
  sort(r.begin(),r.end());
  ll ans = 0;
  ll sum = 0;
  int it = 0;
  for(int i=0;i<n;i++){

  }
}
int main()
{
    cpu();
    int t = 1;
    //cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}

